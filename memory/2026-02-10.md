# 2026-02-10 — Updated Settings & Capabilities

## User Preferences

- **Name:** Rafael Meneses
- **Location:** Fortaleza, Brazil (GMT-3)
- **Vibe:** Prefers automated, low-touch solutions
- **Timezone:** America/Fortaleza

## Audio Transcription

**Whisper installed** — Local speech-to-text (free, offline)

- Model: `tiny` (fastest, good enough for casual)
- Language: Auto-detect or specify `--language pt`
- Script: `transcribe.sh` for quick transcription

**Usage:**
```bash
# Manual transcription
whisper --model tiny --language pt audio.ogg

# Or use the quick script
./transcribe.sh audio.ogg
```

## GitHub Integration

- **Account:** @panthedog
- **Repo:** github.com/panthedog/openclaw-workspace (public, safe)
- **Auth:** OAuth via `gh` CLI

## Installed Skills

| Skill | Purpose |
|-------|---------|
| `git-essentials` | Git workflows |
| `browse` | Browser automation |
| `web-search` | Web search |
| `cli-developer` | CLI development |
| `moltbot-ha` | Home Assistant control |
| `github` | GitHub CLI (bundled) |
| `coding-agent` | Run coding agents (bundled) |
| `skill-creator` | Build custom skills (bundled) |
| `healthcheck` | Security hardening (bundled) |

## Hardening Setup

- **Firewall:** ufw (deny incoming, allow outgoing, allow SSH)
- **Credentials perms:** 700
- **Passwordless sudo:** Enabled for apt/apt-get

## Workspace Backup

- **Repo:** github.com/panthedog/openclaw-workspace
- **Status:** Synced and up to date
- **Safe files only:** Sensitive files excluded via .gitignore

## Scripts Created

| Script | Purpose |
|--------|---------|
| `hardening.sh` | Security hardening |
| `setup-whisper.sh` | Install Whisper + sudo config |
| `transcribe.sh` | Quick audio transcription |

## Commands

```bash
# Sync workspace to GitHub
cd /home/rtiodev/.openclaw/workspace && git add . && git commit -m "update" && git push

# Transcribe audio
./transcribe.sh audio.ogg

# Run hardening
bash hardening.sh
```
